spring.application.name=financetrackerai
server.port=${PORT:8080}

# ── CORS ──
app.cors.front-end-url=https://finance-tracker-frontend-virid.vercel.app

# ── MySQL (Filess.io) ──
spring.datasource.url=jdbc:mysql://1nym1g.h.filess.io:3306/ExpenseTracker_driedplain
spring.datasource.username=ExpenseTracker_driedplain
spring.datasource.password=db07d1db236b374750a7a3580df786babb23bf6d
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ── Security / JWT ──
spring.security.user.name=root
spring.security.user.password=root
jwt.secretKey=7z8eHnQqTz6gVw3p5kF3O0v1bE1s7l9iHj2aC7jN8A8=

# ── Kafka Producer (Confluent Cloud) ──
# Replace the placeholder values below with your actual Confluent Cloud credentials
spring.kafka.bootstrap-servers=${CONFLUENT_BOOTSTRAP_SERVER}
spring.kafka.properties.security.protocol=SASL_SSL
spring.kafka.properties.sasl.mechanism=PLAIN
spring.kafka.properties.sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required username="${CONFLUENT_API_KEY}" password="${CONFLUENT_API_SECRET}";
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
topic.name=t.user.id

# ── Redis (RedisLabs) ──
spring.redis.host=redis-18525.c81.us-east-1-2.ec2.cloud.redislabs.com
spring.redis.port=18525
spring.redis.password=${REDIS_PASSWORD}
spring.redis.username=default
spring.redis.database=0
spring.redis.timeout=120s
spring.redis.ssl.enabled=true

# ── Mail (Mailtrap) ──
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=48a44ae1345d8e
spring.mail.password=0cf4fcd0a4dd52

# ── OpenAI ──
spring.ai.openai.enabled=false

# ── Logging ──
logging.level.com.example=INFO
logging.level.feign.Logger=WARN
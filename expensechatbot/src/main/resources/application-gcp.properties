spring.application.name=expensechatbot
server.port=8084

# ── Ollama LLM ──
ollama.base-url=REPLACE_WITH_OLLAMA_URL
ollama.model-name=qwen2.5:7b

# ── PostgreSQL (Supabase / Neon) ──
spring.datasource.url=REPLACE_WITH_POSTGRES_JDBC_URL
spring.datasource.username=REPLACE_WITH_DB_USERNAME
spring.datasource.password=REPLACE_WITH_DB_PASSWORD
spring.datasource.driver-class-name=org.postgresql.Driver

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ── JWT ──
jwt.secretKey=7z8eHnQqTz6gVw3p5kF3O0v1bE1s7l9iHj2aC7jN8A8=

# ── CORS ──
app.cors.front-end-url=https://finance-tracker-frontend-virid.vercel.app

# ── Logging ──
logging.level.com.example=INFO
import numpy as np

vec1 = np.array([0.2, 0.5, 0.1, 0.4])
vec2 = np.array([0.1, 0.6, 0.2, 0.3])

cos_sim = np.dot(vec1, vec2) / (np.linalg.norm(vec1) * np.linalg.norm(vec2))

print("Cosine Similarity:", cos_sim)
